name: cbse_math_app
description: "CBSE Class 10 Math App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_math_fork: ^0.7.2
  cupertino_icons: ^1.0.6

flutter:
  uses-material-design: true
